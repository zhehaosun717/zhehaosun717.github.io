## 2024-05-24 - Screen Reader Labels for Placeholder Forms
**Learning:** Placeholder text alone is insufficient for screen readers; forms without visible labels must use `.sr-only` label elements explicitly linked via `for` and `id` attributes to ensure proper context.
**Action:** Always verify that inputs have programmatic labels, even if visual design relies entirely on placeholders.
