## 2026-06-15 - Form Accessibility
**Learning:** Screen reader friendly forms that visually rely on placeholders can use the `.sr-only` class to hide explicit labels without compromising the visual layout.
**Action:** Always pair form inputs with explicit `<label>` tags using the `for` attribute mapped to the input's `id`.
